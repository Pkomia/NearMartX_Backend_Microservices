server:
  port: 8763

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://localhost:3306/product_db
    username: root
    password: abc##0987

  jpa:
    hibernate:
      ddl-auto: update

  flyway:
    enabled: true
    url: jdbc:mysql://localhost:3306/product_db
    user: root
    password: abc##0987
