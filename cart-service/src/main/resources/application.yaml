server:
  port: 8764

spring:
  application:
    name: cart-service
  datasource:
    url: jdbc:mysql://localhost:3306/cart_db
    username: root
    password: abc##0987
  
  jpa:
    hibernate:
      ddl-auto: update #prod env use(validate)
  
  flyway:
    enabled: true
    url: jdbc:mysql://localhost:3306/cart_db
    user: root
    password: abc##0987
